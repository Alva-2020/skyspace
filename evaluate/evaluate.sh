# extract embedding 

python make_embedding.py --checkpoints-path "../checkpoints"

# calculate the cosine distance

# calculate the eer results

# calculate the minDCF

#visualize the embeddings with TSNE
